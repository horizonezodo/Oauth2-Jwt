spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oauht2_jwt
    username: root
    password: 123456789
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
       hibernate.format_sql: true
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL8Dialect